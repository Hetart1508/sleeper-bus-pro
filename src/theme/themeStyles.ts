// Central place to import third-party theme styles.
// These imports are kept minimal and safe for Vite (CSS/CSS assets).

import 'bootstrap/dist/css/bootstrap.min.css';
import 'bootstrap-icons/font/bootstrap-icons.css';
import 'animate.css/animate.min.css';

// Optional: add icon fonts / chart CSS if you later wire widgets.
import '@fortawesome/fontawesome-free/css/all.min.css';

// Charts libs currently only require JS; no CSS import needed for basic usage.

